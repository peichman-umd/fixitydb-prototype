[project]
name = "fixitydb-prototype"
version = "1.0.0-dev"
dependencies = [
    "jwcrypto",
    "plastron-models",
    "plastron-repo",
    "python-dotenv",
    "rdflib",
    "requests",
    "requests-jwtauth",
]
